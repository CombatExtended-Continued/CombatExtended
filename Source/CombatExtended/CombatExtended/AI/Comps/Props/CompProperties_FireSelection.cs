using System;
using Verse;

namespace CombatExtended.AI
{
    public class CompProperties_FireSelection : CompProperties
    {
        public CompProperties_FireSelection()
        {
            compClass = typeof(CompFireSelection);
        }
    }
}

