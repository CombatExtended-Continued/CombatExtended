using Verse;

namespace CombatExtended
{
    public class CompProperties_TurretInit : CompProperties
    {
        public CompProperties_TurretInit()
        {
            compClass = typeof(CompTurretInit);
        }
    }
}
